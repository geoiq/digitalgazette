= render :partial => "pages/#{@page_type.underscore}/header"
= render :partial => 'search/banner', :locals => {:page_type => @page_type}
/= render :partial => 'pages/search', :locals => {:users => @user.friends, :groups => @user.groups}
%div#browse
  %h4= "... or Browse #{@page_type.pluralize} by Topic"
= render :partial => 'pages/topics', :locals => { :page_type => (@page_type || "WikiPage") }

- content_for :right_sidebar do
  = link_to(image_tag("add_data_blue.png", :plugin => "digitalgazette", :width => 225, :height => 48, :alt => "Add Data"), "/pages/new",  :style => "display: block; text-align: center; margin-left: -10px", :id => "givenow")
  = box_for :most_viewed, {:limit => 5, :page_types => [@page_type], :load => false}
  = box_for :recent, {:limit => 5, :page_types => [@page_type], :load => false}



  / %div.roundTop.txtDrkGray
  /   %strong= "MOST VIEWED"
  / %div.subPageRightLinks
  /   %ul.dynamicLinkList
  /     - @recent = Page.popular("AssetPage", 5)
  /     - @recent.each do |page|
  /       %li= link_to(h(page.title), page_url(page))
  / %div.roundBtm

  / %div.roundTop.txtDrkGray
  /   %strong= "MOST RECENT"
  / %div.subPageRightLinks
  /   %ul.dynamicLinkList
  /     - @recent = Page.recent("AssetPage", 5)
  /     - @recent.each do |page|
  /       %li= link_to(h(page.title), page_url(page))
  / %div.roundBtm
